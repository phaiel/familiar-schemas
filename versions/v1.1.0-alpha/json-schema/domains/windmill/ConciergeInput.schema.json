{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Input to the concierge agent",
  "properties": {
    "is_first_message": {
      "default": false,
      "description": "Whether this is the first message in a conversation",
      "type": "boolean"
    },
    "request_id": {
      "description": "Request ID for tracing",
      "type": "string"
    },
    "state": {
      "description": "Current agent state"
    },
    "user_message": {
      "default": null,
      "description": "User message content",
      "type": [
        "string",
        "null"
      ]
    },
    "content_length": {
      "description": "Length of the content for routing decisions",
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "request_id",
    "state"
  ],
  "title": "ConciergeInput",
  "type": "object",
  "x-familiar-deprecated": true,
  "x-familiar-kind": "windmill"
}