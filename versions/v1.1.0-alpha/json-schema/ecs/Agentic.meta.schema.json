{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://familiar.dev/schemas/ecs/Agentic.meta.schema.json",
  "title": "Agentic Schema Definition",
  "description": "Schemas that define agent behaviors and autonomous system interactions.",
  "type": "object",
  "required": ["title", "x-familiar-kind"],
  "properties": {
    "title": { "type": "string" },
    "description": { "type": "string" },

    "x-familiar-kind": {
      "const": "agentic",
      "description": "Discriminator for agentic behavior schemas"
    },

    "x-familiar-service": {
      "description": "Service that executes this agentic behavior",
      "type": "object",
      "properties": {
        "$ref": { "type": "string" }
      },
      "required": ["$ref"]
    },

    "x-familiar-meta-schema": {
      "$ref": "#",
      "description": "Self-reference for meta-schema validation"
    }
  }
}
