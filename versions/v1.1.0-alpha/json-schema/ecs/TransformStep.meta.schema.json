{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://familiar.dev/schemas/ecs/TransformStep.meta.schema.json",
  "title": "Transform Step Definition",
  "description": "Pure data transformation using CEL expressions",
  "type": "object",
  "required": ["id", "kind", "output"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z_][a-z0-9_]*$",
      "description": "Unique identifier for this step variable (snake_case)."
    },
    "kind": {
      "const": "transform",
      "description": "Discriminator for transform step"
    },
    "description": { "type": "string" },
    "output": {
      "type": "object",
      "description": "Output object constructed using CEL expressions",
      "additionalProperties": true
    },
    "x-familiar-visual": {
      "type": "object",
      "description": "Visual layout coordinates for flow diagrams",
      "properties": {
        "x": {"type": "number"},
        "y": {"type": "number"}
      }
    }
  }
}
