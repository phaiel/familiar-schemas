{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://familiar.dev/schemas/ecs/Reliability.schema.json",
  "title": "Reliability Guarantee",
  "description": "Reliability characteristics of an action for error handling and retry logic",
  "type": "string",
  "enum": [
    "idempotent",
    "non_idempotent",
    "transactional"
  ],
  "x-familiar-kind": "enum",
  "x-familiar-enum-values": {
    "idempotent": "Can be safely retried (no side effects from duplicates)",
    "non_idempotent": "Cannot be safely retried (duplicate execution causes issues)",
    "transactional": "Must execute within a transaction boundary"
  }
}
