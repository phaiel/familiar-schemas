{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://familiar.dev/schemas/architecture/architecture/steps/Step.meta.schema.json",
  "title": "Step Definition",
  "description": "Base schema for all technique steps in the ISA",
  "type": "object",
  "required": ["id", "kind"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z_][a-z0-9_]*$",
      "description": "Unique identifier for this step variable (snake_case)."
    },
    "kind": {
      "type": "string",
      "enum": ["call", "switch", "map", "parallel", "transform"],
      "description": "Step type discriminator"
    },
    "description": { "type": "string" },
    "x-familiar-visual": {
      "type": "object",
      "description": "Visual layout coordinates for flow diagrams",
      "properties": {
        "x": {"type": "number"},
        "y": {"type": "number"}
      }
    }
  },
  "oneOf": [
    { "$ref": "Callarchitecture/steps/Step.meta.schema.json" },
    { "$ref": "Switcharchitecture/steps/Step.meta.schema.json" },
    { "$ref": "Maparchitecture/steps/Step.meta.schema.json" },
    { "$ref": "Parallelarchitecture/steps/Step.meta.schema.json" },
    { "$ref": "Transformarchitecture/steps/Step.meta.schema.json" }
  ]
}
