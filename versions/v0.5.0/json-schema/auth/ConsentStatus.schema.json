{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConsentStatus",
  "description": "User's current consent status",
  "type": "object",
  "properties": {
    "ai_processing": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "analytics": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "data_sharing": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "marketing_emails": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "privacy_policy": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "terms_of_service": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    }
  }
}